const defaultState = {
    articles: []
};

const blogReducer = (state = defaultState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default blogReducer;